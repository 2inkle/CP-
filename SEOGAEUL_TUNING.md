# 서가을 AI 튜닝 노트

이 문서는 헤드리스 시뮬레이터로 서가을(캐릭터 AI) 성능을 측정하면서 발견한 문제, 수정 내역,
현재 상태, 그리고 앞으로 더 다듬어야 할 부분을 기록한다. 다른 환경에서 이어서 작업할 때
참고할 수 있도록 남겨둔다.

## 1. 헤드리스 시뮬레이터 (`sim.html`)

화면을 그리지 않고 특정 AI 성향으로 다수 국(수백~수천 국)을 자동 진행해 지표를 측정하는 도구.
`js/sim-runner.js`가 `game.js`/`ai.js`/`personas.js`의 전역 함수를 오버라이드해서 동작하며,
원본 게임 파일은 건드리지 않는다.

### 실행 방법 (아무거나 가능)
1. **GitHub Pages**: `https://2inkle.github.io/CP-/sim.html` — 설치 없이 바로 열림
2. **저장소 클론 후 더블클릭**: `git clone https://github.com/2inkle/CP-.git` 받아서 `sim.html`을
   그냥 더블클릭. 순수 `<script src="...">` 태그만 쓰고 fetch/모듈 임포트가 없어서 `file://`로
   열어도 문제없음 (별도 서버 불필요)
3. 로컬 정적 서버(`serve.ps1` 등)에서 열기

### 사용법
- 대상 좌석(분석하고 싶은 성향)과 상대 3자리(매 게임 무작위 배정 또는 고정) 선택
- 목표 국 수 입력 후 "시작"
- 실시간으로 5대 지표가 갱신되고, 국별 결과(목표/샹텐/폴드 여부)가 로그에 남음
- "로그+통계 다운로드"로 실행 전체를 JSON으로 저장 가능. `localStorage`에도 최근 20회
  실행 결과가 자동 보관되어 나중에 다시 확인 가능
- 내장 워치독: 진행이 멎으면 1.5초 이상 진행이 없을 때 자동 감지해서 재시도하고 로그에 상태
  스냅샷을 남긴다. **과거엔 300국당 30~40회씩 개입할 만큼 잦았는데, 원인은 AI 판단이 아니라
  `sim.winDetails` 미초기화 버그(4절 참고)였다** — 그 버그를 고친 뒤로는 300국에 0~1회
  수준으로 줄었다. 그래도 개입 자체가 다시 잦아지면(수십 회 단위) 비슷한 종류의 미초기화/
  undefined 참조 버그가 새로 생겼다는 신호로 의심할 것.

### 5대 지표 정의
| 지표 | 정의 |
|---|---|
| 승률 | 게임(東風戦 1~4국 세트) 중 1위로 마친 비율 |
| 평균타점 | 화료했을 때 획득한 점수(`total`)의 평균 |
| 화료율 | 전체 국 중 본인이 화료(쯔모+론)한 비율 |
| 방총률 | 전체 국 중 본인이 상대에게 론을 당한(방총) 비율 |
| 후로율 | **실제 후로 횟수 / 후로가 가능했던 횟수** (국 단위가 아니라 콜 기회 단위. `aiDecideCallOnDiscard`가 퐁/치가 실제로 가능한 상황에서만 호출된다는 점을 이용해 호출 자체를 "기회"로 집계) |

추가로 "텐파이(유국시) 비율", "폴드(오리) 발동 비율"도 진단용으로 같이 보여준다.

## 2. 발견해서 수정한 버그 (2건)

### 버그 1 — 오리(폴드) 과다 트리거 (발동률 93.3%)
`shouldFold()`가 **상대의 리치**와 **상대가 후로만 2번 한 것**을 똑같이 최고 위협
(`threatLevel >= 2`)으로 취급했다. 아무도 리치를 안 해도 상대 중 누군가 오픈 멜드 2개만
만들면 서가을이 전면 후퇴해버려서, "정말 위험할 때만 포기"라는 설계 의도와 어긋났다.

**수정**: `js/game.js`의 `buildAiContext()` — `shouldFold()`가 `ownShanten >= 1`인 상황에서
전면 오리하려면 **실제 리치 상대(`hasRiichiThreat`)가 있어야만** 하도록 변경. 후로만으로는
약한 안전 선호 정도(기존 `PUSH_FOLD_RULES`의 소극적 가중치)만 적용된다.

### 버그 2 — 국사무쌍 목표 오작동 (300국 중 47% 차지, 텐파이 도달률 2.1%)
국사 판정 조건이 `terminalHonorCount`(요구패 "장수", **중복 포함**)를 기준으로 삼았다.
예를 들어 9m 3장 + 1p 3장(실제로는 2종류뿐)만 있어도 조건(`>= 6`)을 만족해서 국사 모드로
잘못 전환됐다. 게다가 자패/노두패가 전체 136장 중 약 38%라서, 무작위 13장 손패도 우연히
요구패 **종류**가 평균 8~9종 가까이 스치기 때문에 느슨한 기준은 거의 매 국 걸려버린다.
절대 될 리 없는 국사무쌍을 쫓느라 정상적인 화료를 스스로 막고 있었던 것이 저조한 화료율의
핵심 원인이었다.

**수정**:
- `js/hand.js`의 `shantenKokushi()`를 이용해 `js/game.js`의 `buildAiContext()`에
  `kokushiShanten`(실제 국사 샹텐, 종류 수 기준) 필드 추가
- `js/personas.js`의 국사 목표 조건을 `kokushiShanten <= 3 && kokushiShanten <= ownShanten`
  (진짜로 가깝고, 표준형 손패 해석보다 국사 쪽이 객관적으로 더 나을 때만)으로 교체

## 3. 수정 전/후 비교 (각 300국, 상대는 매 게임 무작위 배정)

| 지표 | 수정 전 | 버그 2건 수정 후 |
|---|---|---|
| 화료율 | 8.0% | 13.3% |
| 텐파이(유국시) | 8.3% | 28~45% |
| 오리 발동률 | 93.3% | 30~40% |
| 방총률 | 5.7% | **12.7~17.7%** (악화) |
| 평균순위 | 3.25~3.68 | 3.38~3.68 (거의 개선 안 됨) |
| 승률(1위) | ~2~10% | ~0% (표본 작음, 신뢰도 낮음) |

## 4. 3번째로 발견한 버그 — 진짜 원인은 따로 있었다 (`sim.winDetails` 미초기화)

3절까지의 "방총률 악화 / 순위 개선 안 됨" 결과는 사실 AI 판단 문제가 아니라 **시뮬레이터
자체의 버그** 때문이었다. `js/sim-runner.js`의 `startSim()`이 만드는 `sim` 객체에
`winDetails` 필드가 없는데, `window.resolveWin` 오버라이드(74~99행)의 82행이
`sim.winDetails.push(...)`를 호출한다. 즉 **분석 대상(서가을)이 실제로 화료할 때마다
매번 예외가 터졌다** — 그것도 게임 로직의 실제 `resolveWin`(점수 지급·로그·다음 국 진행)을
호출하기 **직전**에. 그 결과:

- 서가을이 이겨도 점수 지급이 실제로 적용되지 않거나 국 진행이 중간에 끊김
- 화료율 집계(`sim.wins++`)는 예외 발생 전에 실행되므로 카운트는 됐지만, 정작 점수·순위에는
  반영이 안 됨 → **화료해도 순위가 오르지 않는 모순된 결과**
- 이 예외가 매번 러닝하던 `runHand()`의 프로미스 체인을 중간에 끊어놨고, 그 자리를 **기존
  워치독이 계속 대신 복구**해왔다 — 문서 1절에 "근본 원인 미해결"이라 적어뒀던 잦은 워치독
  개입(300국당 30~40회)의 정체가 바로 이것이었다.

**수정**: `startSim()`의 `sim = {...}` 초기화에 `winDetails: []`를 추가(`js/sim-runner.js`).

### 수정 효과 (300국, 서가을 vs 무작위 상대, 2회 반복)

| 지표 | 버그 2건만 수정 (3절) | winDetails 버그까지 수정 (2회 평균) |
|---|---|---|
| 워치독 개입 횟수 | 300국당 30~40회 | 300국당 **0~1회** |
| 화료율 | 13.3% | 13.7% (12.0%, 15.3%) |
| 방총률 | 12.7~17.7% | **11.8%** (13.7%, 10.0%) |
| 평균순위 | 3.38~3.68 | **2.90** (2.93, 2.86) — 1위 실제 발생 |
| 승률(1위) | ~0% | 3회 게임 세트 중 1위 다수 관측 |

**즉, "방총이 늘어서 순위가 안 오른다"는 4절(구)의 진단 자체가 틀렸다** — 서가을의 위험 판단이
얕은 게 아니라, 이길 때 점수가 제대로 안 매겨지고 있었을 뿐이었다. 버그를 고치자 방총률도
오히려 더 낮아졌고(10~14%), 순위는 3.4~3.7에서 2.9 근처로 뚜렷이 좋아졌다.

## 5. 4번째 변경 — 위험도-가치 비교 (`shouldFold()`)

winDetails 버그와 별개로, `js/game.js`의 `buildAiContext()`가 만드는 `ctx.shouldFold()`를
손패 가치까지 반영하도록 바꿨다. 기존에는 `ownShanten`/`threatLevel`/`waitTileCount()`만 보는
이진 임계값이라 치또이 1000점 텐파이와 하네만급 텐파이가 완전히 같은 기준으로 접혔다.

```js
ctx.shouldFold = () => {
  if (hasRiichiThreat && ctx.ownShanten >= 2) return true;
  if (hasRiichiThreat && ctx.ownShanten === 1 && ctx.threatLevel >= 2) {
    if (ctx.bestWinBase() >= 8000) return false;   // 하네만급 이상은 계속 민다
    return true;
  }
  if (ctx.ownShanten === 0) {
    if (ctx.waitTileCount() <= 1) return true;
    if (hasRiichiThreat && ctx.threatLevel >= 2 && ctx.bestWinBase() < 2000 && ctx.waitTileCount() <= 3) return true;
  }
  return false;
};
```

`shouldFold()`는 서가을뿐 아니라 표준형/혼일색/속공형의 `PUSH_FOLD_RULES`/`SPEED_PUSH_RULES`
(공용 판단 레이어)에서도 쓰이므로 이번 변경은 전 페르소나에 적용된다. 표준형 150국 스모크
테스트로 회귀 없음(화료율 32%, 텐파이 100%, 콘솔 에러 없음) 확인.

위 4절의 "수정 후" 두 번 실행(2.93위/2.86위) 자체가 이미 이 변경이 반영된 결과라, 이 변경만의
단독 기여분은 표본 크기상 분리해내기 어렵다 — 다음 튜닝 때 A/B로 나눠 300국씩 비교해볼 것.

## 6. 5절 이후 발견 — "화료율은 낮아도 타점은 높다"가 실제로는 아니었다

5절까지 개선한 뒤 사용자가 직접 확인을 요청해서 `sim.winDetails`(4절 버그 수정으로 이제 정상
동작)를 표준형과 나란히 놓고 비교했다. 결과: 서가을 화료율(13~15%)은 표준형(16%대)보다 낮은데
**평균타점은 오히려 표준형과 비슷하거나 낮았다**(서가을 3800대 vs 표준형 3885). "화료율은
낮아도 타점은 높다"는 캐릭터 설계 의도가 데이터로 확인되지 않았다.

원인은 두 가지였다.

### 6-1. 타점 계산 단위가 실제 정산액이 아니었다
`bestWinBase()`(리치 여부 판단에 쓰는 예상 타점)가 `js/yaku.js`의 `scorePoints()`가 반환하는
`base`(표 단위 — 만관=2000, 하네만=3000, 바이만=4000, 산바이만=6000, 역만=8000)를 그대로 쓰고
있었다. 이건 실제로 손에 들어오는 점수(비딜러 만관=8000/12000, 하네만=12000/18000 ...)가
**아니라** 점수표 내부 계산용 중간값이라, "8000이면 하네만급"처럼 실제 점수 감각으로 비교하면
완전히 다른 얘기가 된다(5절에서 넣은 `shouldFold()`의 8000/2000 임계값도 이 착각 위에 있었다).

**수정**: `js/game.js`의 `estimateBestWinValue()`가 `r.base` 대신 `r.total`(그 손패로 화료했을 때
실제로 거두어들이는 점수, 쯔모 총합 기준)과 `r.han`을 함께 추정하도록 바꿨다. `ctx.bestWinBase()`는
이제 실제 정산 점수(비딜러 만관=8000 등)를 반환하고, 새 `ctx.bestWinHan()`이 판수(리치 제외)를
반환한다. `shouldFold()`의 임계값도 같은 단위로 재보정했다(하네만급 8000→**12000**, 만관
2000→**8000**).

또한 `js/sim-runner.js`의 승리 기록도 손봤다 — `win.total`은 **항상 쯔모라면의 총합 공식**이라
론으로 이겼을 때도 그 값을 그대로 쓰면 실제로 받은 금액보다 부풀려진다. 이제 론이면
`payments.ron`, 쯔모면 `total`을 쓰고 혼바 보너스(300점 × 혼바 수)까지 더해 "그 국에 실제로
거두어들인 점수"를 기록한다.

### 6-2. 리치를 너무 일찍(싼 손패로) 걸고 있었다
기존 리치 규칙(`bestWinBase() <= 2000 → dama`, 즉 표 단위 "만관 이하면 보류")은 리치 자신의
1판·일발·우라도라 가능성을 전혀 셈하지 않은 맨몸 타점이 만관을 넘을 때만 다마를 풀었다.
맨몸으로 만관을 넘기는 손패는 드물기 때문에, 실제로는 대부분의 손패가 계속 다마 상태로 남다가
상대가 먼저 흘린 패로 그냥 싸게(1100~2000점) 론을 받는 경우가 절반 가까이 됐다(직전 대화의
`goal=normal` 화료 40여 건 중 1~2판짜리가 절반).

**수정**: `js/personas.js`의 서가을 `riichiRules` 마지막 규칙을
`{ when: (c) => (c.bestWinHan() + 1) < 4, decision: 'dama' }`로 바꿨다. "지금 역+도라 판수(리치
제외)에 리치 한 판을 더해 4판(하네만권)에 못 미치면 계속 보류, 도달하는 순간 리치"라는 뜻이다.
사용자가 지정한 정확한 기준.

### 6-3. 치또이쯔 목표가 과다 트리거되고 있었다
기존 조건은 `pairCount >= 3 && bodyCount < 2` — `pairCount`는 "2장 이상인 종류의 개수"만 셀 뿐
**종류 부족 페널티나 표준형 샹텐과의 비교가 전혀 없어서**, 국사 버그(2절)와 똑같은 종류의
과다 트리거였다. 무작위 13장 손패도 페어 3개 정도는 흔히 생기기 때문에, 표준형으로 풀면 훨씬
빠른 손패까지 치또이로 끌고 가는 경우가 로그에서 자주 보였다.

**수정**: `js/hand.js`의 `shantenChiitoi()`(7종류 페어 기준 실제 치또이 샹텐, 종류 수 부족
페널티 포함)를 `js/game.js`의 `buildAiContext()`에 `chiitoiShanten` 필드로 노출하고, 조건을
`chiitoiShanten <= 3 && chiitoiShanten <= ownShanten && bodyCount < 2`로 바꿨다. 국사 버그를
고쳤을 때와 정확히 같은 패턴(전용 샹텐 계산 + 표준형 샹텐과 비교)이다.

### 6-4. 세 가지를 함께 적용한 결과 (500국, 서가을 vs 무작위 상대)

| 지표 | 5절까지 (6절 수정 전) | 6절 수정 후 |
|---|---|---|
| 화료율 | 12~15.6% | 13.0% (65/500) |
| 방총률 | 10~14% | 11.6% |
| 평균순위 | 2.9 | **2.73** |
| 승률(1위) | 드묾 | 12.5% (10/80게임) |
| **평균타점** | 3800대 | **5957** |
| **화료 중앙값(median)** | 2000 | **4000** |
| chiitoi 목표 선택 비율 | (미측정) | 500국 중 52국(10.4%) |
| kokushi 목표 선택 비율 | (미측정) | 500국 중 1국(0.2%) |

목표(goal)별 승리 평균타점(6-4 실행분): `normal` 4884, `tanyao` 5938, `chanta` **8963**(만관급),
`chiitoi` **11000**(하네만~바이만급). 특히 `chanta`는 5절 이전 대화에서 확인했던 1500점대에서
9000점 가까이로 뛰었다 — 리치 타이밍 수정이 정확히 의도한 효과를 냈다는 뜻이다. 화료율·방총률은
그대로인데 평균타점만 거의 2배로 뛰고 순위(2.73)까지 더 좋아졌다.

표준형 150국 스모크 테스트(화료율 27.3%, 방총 10.7%, 텐파이 75%, 콘솔 에러 없음)로 회귀 없음
확인.

## 7. 절일문(수트 끊기) 강도 재검토

6절 다음으로 사용자가 직접 지적한 문제: `cutSuit` 관련 가중치가 `SHANTEN_PENALTY`(15)에 맞먹는
크기(+10, +10 → 최대 +20)라서, **이 두 규칙만 보면 최선 샹텐보다 1 나빠지는 버림도 골라버릴
수 있었다**(`aiChooseDiscard`가 모든 후보를 점수순으로 정렬하지 최선 샹텐 후보로만 제한하지
않기 때문). 게다가 이미 완성된 몸통(커쯔/슌쯔)인지 여부(`isPartOfCompleteSet`)도 전혀 확인하지
않아서, 절일문 대상 수트에 이미 완성된 몸통이 있어도 그 패까지 절일문 명목으로 버려질 수 있는
구조였다.

**수정**(`js/personas.js`, 서가을 `normal` 목표의 절일문 두 규칙): 두 조건 모두에
`!c.isPartOfCompleteSet(t)`(완성된 몸통은 그 수트라도 건드리지 않음)와
`resultShanten <= c.ownShanten`(이 버림으로 샹텐이 최선보다 나빠지면 가중치 자체를 안 줌)를
추가했다. 절일문은 손패 정리 수단이지 그 자체가 목적이 아니므로, 이미 만들어진 몸통이나
샹텐을 희생하면서까지 강행하지 않도록 명시적으로 막은 것이다.

### 결과 (500국, 서가을 vs 무작위 상대, 6절 수정 이후 기준 비교)

| 지표 | 6절까지 | 7절 수정 후 |
|---|---|---|
| 화료율 | 13.0% (65/500) | **16.4%** (82/500) |
| 방총률 | 11.6% | 13.6% |
| 평균순위 | 2.73 | **2.68** |
| 승률(1위) | 12.5% (10/80게임) | **19.8%** (16/81게임) |
| 평균타점 | 5957 | 4693 |
| goal=normal 평균 최종 샹텐 | (미측정) | 0.66 (225국 기준) |

화료율·승률·순위 모두 개선됐다(방총률은 소폭 상승했지만 화료 자체가 늘어난 트레이드오프로
보인다). 평균타점은 오히려 낮아졌는데, 이는 나쁜 신호라기보다 **샹텐을 희생해가며 억지로
수트를 끊던 손패 중 일부가 끊지 않고 더 빨리 화료로 이어지면서, 평균에 섞이는 "빠르지만
값싼" 화료 비중이 늘어난 것**으로 해석된다(화료율이 13.0%→16.4%로 26% 가까이 늘었으니
전체 기대타점 총량은 오히려 늘었을 가능성이 높다). 표준형/혼일색/속공형/수비형은 이 규칙과
무관해서(절일문은 서가을 전용) 별도 회귀 테스트는 생략했다.

## 8. 역패 비축 로직 — 위험 상황 한정 + 자패 전체로 확대

기존 규칙은 `isYakuhaiTile(...) && discardedCountOf(t) >= 2`(역패가 2장 이상 버려지면 위험
상황과 무관하게 항상 비축)였다. 사용자 지적대로 이건 두 가지가 어색했다: ① 위험하지 않을 때도
역패를 붙들고 있을 이유가 없고(그 시간에는 위 -5 역패 선호나 손패 효율에 맡기는 게 낫다),
② "역패"로 한정돼 있어서 자신의 역이 안 되는 손님 바람패(예: 자신은 동가인데 남가패를 쥔 경우)는
2장 이상 버려졌어도 안전패로 취급되지 않았다 — 위험할 땐 역이 되느냐보다 쏘이지 않느냐가
우선인데 그 구분이 빠져 있었다.

**수정**(`js/personas.js`): 조건을 `c.threatLevel >= 1 && t.suit === 'z' && c.discardedCountOf(t) >= 2`로
바꿨다 — 위험 상황(threatLevel>=1, 리치 또는 다수 후로 감지)에서만 발동하고, 역패 여부와
무관하게 2장 이상 버려진 자패라면 전부 비축 대상이 된다.

### 결과 (500국 × 3회, 서가을 vs 무작위 상대)

| 실행 | 화료율 | 방총률 | 평균순위 | 승률(1위) |
|---|---|---|---|---|
| 1회 | 14.4% | 15.6% | 2.81 | 16.7% |
| 2회 | 13.4% | 16.0% | 2.85 | 12.7% |
| 3회 | 15.6% | 13.6% | 2.65 | 25.6% |
| 7절(수정 전, 1회 실행) | 16.4% | 13.6% | 2.68 | 19.8% |

3회 실행 간 편차(방총률 13.6~16.0%, 평균순위 2.65~2.85, 승률 12.7~25.6%)가 이 규칙 하나로
기대할 수 있는 효과보다 훨씬 커서, **이번 변경 단독의 실측 효과는 500국 표본으로는 노이즈에
묻혀 판별하기 어렵다.** 다만 로직 자체는 사용자 의도(위험할 때만, 역패에 한정하지 않고)를
정확히 반영하고, 안전 상황에서 불필요하게 역패를 쥐고 있던 비효율을 제거하므로 방향은 맞다고
판단된다. 회귀(화료율이 크게 떨어지거나 콘솔 에러)는 없음을 확인. 표본을 1000~2000국 이상으로
늘려 다시 비교해보는 게 이 변경의 실효를 판별하는 다음 단계다.

## 9. 전체 재검토 — "화료율 20%, 방총률 유지, 순위 개선" 목표

사용자 요청("화료율을 20%에 가깝게, 방총률은 그대로, 평균 순위를 높이려면")에 따라 전체를
다시 검토했다. **표본을 1000국으로 올리고, 표준형을 같은 조건에서 돌려 기준선으로 삼았다.**

### 9-1. 기준선 — 표준형 @1000국 (이 엔진에서 달성 가능한 상한)

| 지표 | 표준형 | 서가을(당시) |
|---|---|---|
| 화료율 | **22.5%** | 14.5% |
| 방총률 | 12.3% | 15.1% |
| 평균순위 | 2.44 | 2.77 |
| 후로율 | **76.2%** | 41% |
| 유국텐파이 | **76.1%** | 44% |
| 폴드 발동률 | **48.0%** | 43% |

여기서 중요한 발견: **폴드 발동률은 오히려 표준형이 더 높다(48% vs 43%).** "서가을이 너무 자주
접어서 화료율이 낮다"는 가설은 틀렸다. 진짜 격차는 **손패 진행 속도**(후로율 76% vs 41%,
유국텐파이 76% vs 44%)였다.

### 9-2. 넣은 변경 5가지

1. **반오리(`guard`) 티어 신설** — 기존엔 `shouldFold()`가 걸리면 곧장 전면 배타오리(±100)라
   손패를 통째로 버렸다. 이제 정말 가망 없는 경우(3샹텐 이상 / 9순 이후 / 죽은 텐파이)만
   `fold`로 보내고, 그 사이(주로 이른 순목의 1~2샹텐)는 `guard`(±10/±4)로 보내 손패를 살린다.
2. **밀 때의 상시 약한 수비(±5)** — `normal` 등 공격 모드엔 위험도 규칙이 아예 없어서
   미는 동안 방총을 줄일 수단이 없었다. 합이 `SHANTEN_PENALTY`(15)보다 작아 샹텐·우케이레는
   전혀 깎지 않고 "효율이 같을 때만 안전한 쪽"으로 기운다(표준형 `PUSH_FOLD_RULES`와 같은 취지).
3. **형식텐파이** — `wallRemaining <= 16 && ownShanten <= 1`이면 오리를 풀고 텐파이를 노린다.
   노텐 벌부는 확정 손실인데 막판엔 상대가 화료할 턴도 얼마 남지 않았다.
4. **`isBehind` 조건 강화** — 기존 기준(4위이거나 1등 아니면서 18000점 미만)은 첫 국부터 거의
   매 국 걸려서 느린 대요구/탕야오로 자주 갈아탔다(500국 중 95국). `(4위 && 3국 이후) ||
   15000점 미만`으로 좁혔다.
5. **효율 세금 완화** — 절일문 가중치 10+10 → 5+3(우케이레 보정이 장당 0.5점이라 20 앞에서는
   늘 무시됐다), 치또이 문턱 `<=3` → `<=2`, 4장 보유 유지 -25 → -12(`SHANTEN_PENALTY`를 넘겨
   샹텐을 희생시키고 있었다).

### 9-3. 그 과정에서 찾은 버그 — 역 없는 텐파이가 영영 리치를 못 걸고 있었다

5번(절일문 약화)을 넣자 화료율이 16.6% → 13.0%로 **떨어졌다.** 원인은 6절에서 넣은 리치 규칙:
`bestWinHan()`은 역이 하나도 없으면 **0**을 반환하는데, 규칙이 `bestWinHan() + 1 < 4 → dama`라
**역 없는 텐파이는 영구히 리치를 못 걸었다 = 화료 자체가 불가능**했다. 절일문(혼일색·역패의
공급원)을 약화시키자 역 없는 텐파이가 늘면서 이 결함이 드러난 것이다.

**수정**: `{ when: (c) => c.bestWinHan() === 0, decision: 'riichi' }`를 다마 규칙 **앞에** 추가.
역이 없으면 판수와 무관하게 무조건 리치. 이것만으로 화료율 13.0% → **17.2%**.

### 9-4. 결과 (각 1000국)

최종 조합을 1000국씩 **2회** 돌려 안정성까지 확인했다(괄호 안이 2회차).

| 지표 | 변경 전(3×500) | 변경 후(1000국 ×2) | 표준형(1000국) |
|---|---|---|---|
| 화료율 | 14.5% | **17.6% / 17.8%** | 22.5% |
| 방총률 | 15.1% | **13.6% / 13.2%** | 12.3% |
| 평균순위 | 2.77 | **2.58 / 2.53** | 2.44 |
| 승률(1위) | 18.3% | **27.6% / 23.9%** | 30.6% |
| 평균타점 | ~5600 | 6135 / 5260 | 5265 |
| 유국텐파이 | 44% | 59.8% / 66.8% | 76.1% |
| 후로율 | 41% | 49.0% / 51.7% | 76.2% |
| 폴드 발동률 | 43% | 40.5% / 44.9% | 48.0% |

화료율·방총률·순위가 **동시에** 개선됐고 2회 실행 모두 일관됐다. 다만 목표였던 화료율 20%에는
2.2~2.4%p 못 미친다.

### 9-5. 20%에 도달하지 못한 이유 (구조적)

남은 격차의 대부분은 **캐릭터 설정 그 자체**다. 서가을은 전체 국의 약 1/3을 후로를 막거나
느린 형태를 쫓는 모드(치또이 / 대요구 / 탕야오 / 오리·반오리)에서 보내고, `normal` 모드에서도
절일문·커쯔 우선·역패 유지 같은 효율 세금을 낸다. 표준형은 이런 제약이 하나도 없다.
여기서 20%까지 더 올리려면 다음 중 하나를 **캐릭터를 깎아가며** 선택해야 한다:

- 치또이/대요구 모드를 더 줄이거나 없애기 (가장 큰 효과, 캐릭터 손상도 가장 큼)
- `커쯔 우선`(연결패 단독 +4) 폐지 — 슌쯔가 커쯔보다 빠르다
- 다마 유지(4판) 문턱을 낮춰 리치를 더 자주 걸기
- 후로 정책을 표준형 수준으로 개방

## 10. 목표를 "일반 플레이어 수준"으로 재설정 (사람 통계와의 비교)

사용자가 지표를 실제 사람 통계와 비교해 달라고 해서 정리한 뒤, **"일반 플레이어 정도"를 목표**로
다시 다듬었다(리치율·평균 화료 순목은 측정하지 않기로 함).

### 10-1. 비교 기준과 주의사항
- **후로율은 비교 불가** — 우리 지표는 "실제 후로/후로 가능 횟수"인데 통상 통계의 副露率은
  "한 번이라도 후로한 국의 비율"(보통 30~35%)이다. 정의가 달라 직접 비교하면 안 된다.
- **평균순위·승률은 상대값** — 봇 3명이 상대라 그 필드에서의 상대 성적일 뿐이다(4인 평균은
  정의상 항상 2.50).
- 아래 사람 기준값은 천봉/작혼 계열에서 흔히 인용되는 대략치로 ±1%p 오차를 감안할 것.

| 지표 | 일반 플레이어 | 상급자 |
|---|---|---|
| 화료율 | ~20% | 21~23% |
| 방총률 | ~14% | 11~12% |
| 평균타점 | ~5,800 | ~6,000 |

### 10-2. 추가로 찾은 결함 — 론 기준 역 판정
9-3절에서 "역이 없으면 리치"를 넣었지만 판정에 쓴 `bestWinHan()`은 **쯔모 기준**이라
멘젠쯔모 1판이 포함된다. 즉 **역이 멘젠쯔모뿐인 손패는 판수 1로 잡혀 다마에 머물렀고, 그런
손패는 론으로는 화료가 불가능**하다(쯔모로만 이길 수 있어 화료 기회의 대부분을 스스로 버림).

**수정**: `estimateBestWinValue()`가 같은 손패를 론으로 평가한 판수 `hanOnRon`도 함께 계산하고,
`ctx.bestWinHanOnRon()`으로 노출. 리치 규칙의 예외를 `bestWinHanOnRon() === 0`으로 교체했다.
단독 효과: 화료율 18.1% → **18.9%**(1000국).

### 10-3. 목표별로 쪼개 본 진단
`handRecords`를 목표별로 집계하니 원인이 분명했다:

| 목표 | 국수 | 화료율 | 방총률 |
|---|---|---|---|
| normal | 744 | **21.5%** | 11.7% |
| chiitoi | 59 | 20.3% | 13.6% |
| chanta | 41 | **12.2%** | **29.3%** |
| fold | 83 | 4.8% | 14.5% |

**normal 모드는 이미 21.5%로 일반 플레이어 수준을 넘는다.** 전체를 끌어내린 건 대요구(chanta) —
화료율은 절반인데 방총률은 2.5배다. 원인은 명확했다: 대요구는 중장패(4·5·6)를 +30으로 강제
방출하는데, 그게 리치 상대에게 **가장 위험한 패**다.

**수정 2가지**: ① 중장패 강제 방출에 `threatDangerFor(t) < 2` 게이트를 달아 위험패는 강제로
버리지 않게 함 ② 대요구 진입 조건을 `terminalHonorCount >= 4`로 올려(기존 `<= 5`) 요구패가
어중간한 손패는 기본형으로 두게 함.

### 10-4. 시도했다가 되돌린 것
- **배타오리 문턱 9순 → 11순**: 방총률이 12.9% → 15.2%로 뛰고 화료율은 그대로여서 되돌렸다.
  "더 오래 밀면 화료가 는다"는 직관이 이 엔진에서는 성립하지 않는다.
- **`shouldFold()`의 1샹텐 가치 문턱 12000 → 8000**: 측정상 개선처럼 보였으나
  **실제로는 죽은 코드**임을 확인했다 — `bestWinBase()`는 텐파이 후보가 있을 때만 값이 나와서
  1샹텐에서는 항상 0이라 어떤 문턱을 넣어도 결과가 같다. 되돌리고 주석으로 남겨뒀다.
  (그때 관측된 20.3%는 실행 간 편차였다.)

### 10-5. 최종 결과 — 같은 설정 3회(3000국) 합산

| 지표 | 9절 시점 | **최종** | 일반 플레이어 | 표준형(참고) |
|---|---|---|---|---|
| 화료율 | 17.7% | **19.2%** | ~20% | 22.5~23.7% |
| 방총률 | 13.4% | **12.8%** | ~14% | 12.3~15.3% |
| 평균순위 | 2.55 | **2.39** | (상대값) | 2.33~2.44 |
| 평균타점 | ~5,700 | ~5,650 | ~5,800 | ~5,000 |
| 화료율−방총률 | +4.3 | **+6.4** | +6 내외 | +8~11 |

화료율 19.2%는 일반 플레이어(~20%) 바로 아래, 방총률 12.8%는 일반 플레이어(~14%)보다 좋고,
**공수 밸런스(+6.4)는 일반 플레이어 기준(+6)과 일치**한다. 목표한 프로필에 도달했다고 본다.
개별 1000국 실행의 편차가 ±1.1%p 수준이라, 이보다 작은 차이를 쫓는 튜닝은 의미가 없다.

## 11. 남은 문제 — 서가을 AI를 더 다듬어야 함

현재(10-5) 프로필이 목표한 "일반 플레이어 수준"에 도달했으므로, 아래는 급하지 않은 후속 과제다.

### 다음에 시도해볼 만한 방향 (미착수)
- **`shouldFold()`의 1샹텐 가치 조건이 죽은 코드**(10-4 참고) — `bestWinBase()`가 1샹텐에서
  항상 0이라 아무 효과가 없다. 1샹텐에서도 의미 있는 가치 지표(예: `doraCountInHand`, 확정
  역패 커쯔 보유 여부)로 바꾸면 "큰 손패는 1샹텐에서도 민다"는 판단이 실제로 작동하게 된다.
- **`guard`(반오리) 모드의 성적이 나쁘다** — 최근 측정에서 28국 중 화료 0, 방총 17.9%였다.
  표본이 작아 단정할 수 없지만, 반오리 구간의 콜 정책(현재 "샹텐이 확실히 줄 때만")과
  가중치(±7)를 다시 볼 여지가 있다.
- **화료율을 20% 위로 더 올리려면** 9-5의 캐릭터 항목(치또이/대요구 모드 축소, 커쯔 우선 폐지,
  후로 개방) 중 어디까지 양보할지 결정해야 한다.
- 튜닝할 때마다 `sim.html`로 300국 이상 돌려서 5대 지표 + `handRecords`의 목표별 분포를
  같이 확인하며 회귀가 없는지 볼 것. **워치독 개입 횟수가 다시 눈에 띄게 늘어나면(수십 회
  단위) 새로운 미초기화/undefined 참조 버그가 생겼다는 신호로 우선 의심할 것** — 이번
  사례처럼 겉보기엔 "AI 성향 문제"로 보이는 이상 수치가 실은 하네스 버그였을 수 있다.
- **페이지를 새로고침하지 않고 "시작"을 여러 번 누르면 안 됨**: `setupSimOverrides()`가
  멱등하지 않아서(이번에 가드를 추가해 고쳤지만) 반복 실행 시 오버라이드가 여러 겹 쌓여
  `handRecords`/`winDetails` 같은 원본 배열에 같은 국이 중복으로 들어갈 수 있었다. 지금은
  `window.__simOverridesInstalled` 가드로 막아뒀지만, 신뢰할 수 있는 수치가 필요하면 항상
  페이지를 새로고침한 뒤 "시작"을 한 번만 누르는 습관을 유지할 것.

## 12. 참고 — 진단에 쓴 방법

`sim.html`에서 300국을 돌리고 `sim.handRecords`를 목표(goal)별로 집계해서 각 목표의
평균 최종 샹텐·화료 수·텐파이 도달률을 비교하는 방식으로 국사 버그를 찾아냈다. 예:

```js
const byGoal = {};
for (const r of sim.handRecords) {
  const g = r.finalGoal || 'null';
  if (!byGoal[g]) byGoal[g] = { count: 0, shantenSum: 0, wins: 0, tenpaiAtEnd: 0 };
  byGoal[g].count++;
  if (r.finalShanten != null) byGoal[g].shantenSum += r.finalShanten;
  if (r.outcome === 'win-tsumo' || r.outcome === 'win-ron') byGoal[g].wins++;
  if (r.finalShanten === 0) byGoal[g].tenpaiAtEnd++;
}
```

새 AI를 추가하거나 기존 성향을 튜닝할 때, 이 방식(목표/상황별로 쪼개서 비교)이 가장 빨리
원인을 찾는 데 효과적이었다.
